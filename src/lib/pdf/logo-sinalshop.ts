// Logo da Sinalshop em base64 (data URI) para o cabeçalho do PDF.
// Enquanto o usuário não enviar o PNG, fica null → o documento renderiza o
// cabeçalho da Sinalshop só com texto (razão social + CNPJ + endereço).
export const LOGO_SINALSHOP: string | null = null;
